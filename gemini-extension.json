{
  "name": "local-rag",
  "version": "2.0.0",
  "description": "Persistent semantic memory + code RAG as an MCP server for AI agents — recall decisions, search code by meaning, and build continuity across sessions",
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "Memory Server URL",
      "description": "URL of the running local-rag HTTP server (default: http://localhost:7531)",
      "envVar": "MEMORY_SERVER_URL"
    }
  ],
  "mcpServers": {
    "local-rag": {
      "command": "node",
      "args": [
        "${extensionPath}/hooks/mcp-proxy.mjs"
      ],
      "cwd": "${extensionPath}",
      "env": {
        "CLAUDE_PROJECT_DIR": "${workspacePath}"
      }
    }
  }
}
